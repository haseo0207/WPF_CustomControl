<UserControl
    x:Class="Demo.Views.PaginationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="clr-namespace:CustomControls.Components;assembly=CustomControls"
    xmlns:vm="clr-namespace:Demo.ViewModels">

    <UserControl.DataContext>
        <vm:PaginationViewModel />
    </UserControl.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            FontSize="20"
            FontWeight="Bold"
            Text="Pagination" />

        <!--  Settings  -->
        <StackPanel
            Grid.Row="1"
            Margin="0,16,0,12"
            Orientation="Horizontal">
            <TextBlock VerticalAlignment="Center" Text="Items per page: " />
            <TextBox
                Width="50"
                VerticalAlignment="Center"
                Text="{Binding ItemsPerPage, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding TotalPages, StringFormat='Total Pages: {0}'}" />
            <TextBlock
                Margin="20,0,0,0"
                VerticalAlignment="Center"
                Text="{Binding CurrentPage, StringFormat='Current Page: {0}'}" />
        </StackPanel>

        <!--  ListView  -->
        <ListView
            Grid.Row="2"
            Margin="0,0,0,12"
            ItemsSource="{Binding PageItems}" />

        <!--  Pagination  -->
        <cc:Pagination
            Grid.Row="3"
            HorizontalAlignment="Center"
            CurrentPage="{Binding CurrentPage}"
            TotalPages="{Binding TotalPages}" />
    </Grid>
</UserControl>
